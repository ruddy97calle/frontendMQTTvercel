import React, { useState, useEffect } from 'react';function CepilladoEstado() {  const [estado, setEstado] = useState(null);  const consultarEstado = async () => {    try {      const response = await fetch(`${process.env.REACT_APP_BACKEND_URL}/cepillado/estado`);      if (!response.ok) {        throw new Error('Error al consultar el estado');      }      const data = await response.text();      setEstado(data);    } catch (error) {      console.error(error.message);    }  };  useEffect(() => {    const intervalId = setInterval(consultarEstado, 100);      return () => clearInterval(intervalId);    }, []);  return (    <button className={`btn-class-name ${estado === 'ENCENDIDO' ? 'active' : ''}`}>      <span className="back"></span>      <span className="front"></span>    </button>  );}export default CepilladoEstado;